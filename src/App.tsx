import Routing from "./routes/Routing";

const App = () => {
  return (
    <main className="relative min-h-screen w-screen overflow-hidden">
      <Routing/>
      
    </main>
  );
};

export default App;
